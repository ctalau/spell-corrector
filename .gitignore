# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.venv/
venv/
.env
.env.*

# Tests / tools
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/
.tox/

# Editors
.idea/
.vscode/
*.swp

# Data that must not be committed
data/raw/
data/bea60k/
# Derived from the benchmark files above; regenerated by scripts/distill/build_data.py
data/distill/
data/cache/
# Prompt-optimization dev set: regenerated deterministically by
# spelling_reranker/dev_set.py (seeded), and it caches a corpus slice.
data/dev_set/
*.zip

# Frozen-encoder feature caches (multi-GB). Manifests/hashes live in artifacts/frozen.
artifacts/frozen_cache/

# Local logs / scratch
*.log
tmp/
.cache/
.venv/

# OS
.DS_Store

# Mid-run student checkpoints pulled off the pod; the kept ones are committed
# explicitly under artifacts/spell_slm_m7/.
artifacts/distill_checkpoints/
