# Experiment 2 — operational notes, bug post-mortems, budget

These are the durable parts of the old root-level `HANDOFF.md` (a session handoff
written *before* experiment 2 produced a number). The result itself is in
[README.md](README.md); the rationale is in [PLAN.md](PLAN.md). Everything below
is preserved as written at the time — including the things that went wrong.

Branch at handoff: `claude/model-training-75-percent-k0bqon`. Last commit at
handoff: `09b7978` (the run that OOMed). The successful run is commit
`b9b66cf275a062390e4428674eb27396461b1dcb`, pod `xwvjd2w980kk00`.

## 1. Benchmark integrity — important

The typo generator is calibrated against **Wikipedia's public common-misspellings
list** (`data/wikipedia_misspellings.txt`, vendored, CC BY-SA), *not* BEA-60K.
An earlier draft calibrated against BEA statistics; the repo's own leak test
caught it. `tests/test_dataset.py::test_training_construction_does_not_reference_locked_benchmark`
fails the build if any training-construction file so much as mentions the
benchmark. **Keep it that way.** `gold0_fraction` and `context_noise_prob` were
fixed a priori from the structure of the task, never tuned against BEA.

Calibration result (`reports/typo_calibration.json`):

| | Authentic (Wikipedia) | Synthetic |
|---|---|---|
| ED1 | 72.55% | 73.28% |
| ED2 | 25.01% | 21.80% |
| ED3+ | 2.44% | 4.92% |

## 2. The scope decision behind the ceiling

**Hunspell is fixed as the only candidate generator** (an explicit user
decision — Aspell was ruled out). That pins pool coverage at a hard ceiling
of ~81%.

**Be honest about this in the writeup: 92.4% conditional is a stretch and the
run may land short (a 72-75% result is a realistic outcome).** Do not present a
near-miss as a success. Measured for reference on a 6,000-error BEA sample: a
Hunspell ∪ Aspell pool would have raised the ceiling to 87.7%, which is the
single biggest lever available — but it is out of scope by user decision.

Do not quietly widen the candidate generator to hit the number: that was ruled
out by the user, and the honest 72-75% result with the ceiling explained is more
useful than a number obtained by changing the task.

## 3. How the run is driven (Runpod operational knowledge)

The controlling sandbox has **outbound HTTPS only — SSH to the pod is blocked**.
The pod is therefore self-driving: `launch.py` sets the container entrypoint to
`bootstrap.sh`, which clones, sets up, runs everything, and serves progress and
artifacts over Runpod's HTTP proxy.

```bash
python scripts/runpod/launch.py \
  --name spell-corrector-exp2 --disk-gb 100 \
  --branch claude/model-training-75-percent-k0bqon \
  --target-train 3000000 --target-valid 60000
```

Monitor (pod id from launch output):

```
https://<pod-id>-8000.proxy.runpod.net/run.log
https://<pod-id>-8000.proxy.runpod.net/STATUS   # experiment | SUCCESS | FAILED: ...
https://<pod-id>-8000.proxy.runpod.net/DONE     # appears when finished
```

Pull results back, then **always** terminate:

```bash
python scripts/runpod/fetch_artifacts.py <pod-id> --dest .
python scripts/runpod/terminate.py --all
```

`RUNPOD_KEY` is in the environment. **Pods bill per second for as long as they
exist** — terminate on every exit path, including failures.

Phase timings observed on an L40S / 28 vCPU pod:

| Phase | Time |
|-------|------|
| Setup (apt+pip+corpus, parallel) | 15 s |
| Unit tests | 40 s |
| Preflight (configs on GPU) | 5 s |
| Typo calibration | ~3.5 min |
| Data build | ~19 min |
| Sanity train | ~1 min |
| Full train (87M, ~8.1k steps) | ~3-4 h (unverified at time of writing; the successful run took 3.11 h) |
| BEA-60K benchmark | ~15 min (unverified at time of writing) |

## 4. The run that died: pod `3gxtgufkzcvl40`, commit `09b7978`

The last run before the successful one got **all the way into the full train and
died at step 18 of 8,130 with a CUDA OOM** in backward. The pod was terminated.
At the time of the handoff there was no trained model and no BEA number.

What that run did establish, all reproducible:

| | |
|---|---|
| Setup | 15 s |
| Preflight (2 configs on GPU) | 5 s |
| Calibration | ~3.5 min |
| Vocabulary: 525,599 eligible types → 60,000 | 111 s |
| Typo table: 46,825 words, 410,555 unique typos | 583 s |
| Example instantiation: 2,081,171 train / 20,773 valid | 465 s |
| Sanity train (28M) | 4.7 s |
| Full train (87M), throughput | ~1.4 steps/s → ~1h40m for 8,130 steps |

Dataset statistics ([`data_stats.json`](data_stats.json), also kept in
[`reports/run3_partial/`](../../run3_partial/), and regenerated by any build):

- context noised: 24.6% (target 25%)
- mean serialized length 203 bytes, p95 337, max 448
- gold at slot 0: 76.6%, slots 1-9: 22.6%, slots 10-15: 0.8%

### A result worth keeping: the data distribution is right

The raw generator produces ED1/ED2/ED3+ of 73/22/5, but the training set after
the "gold must be in the Hunspell pool" filter is **81.7 / 16.9 / 1.4**. That is
not drift — it is the filter re-weighting the data to match the population the
model is actually scored on. The BEA errors where gold *is* in the pool are
**83.0 / 15.7 / 1.3**. The match is close to exact, which is the strongest
available evidence that the data fix addresses experiment 1's core defect.

### The one number that still looks wrong

`gold@0` is 76.6% in training but 66.8% in the BEA conditional population, so
easy examples are still over-represented. Cause: `gold0_fraction` budgets
against the *target* count (3M), not the count actually produced (2.08M), so the
cap never bound. Fix by budgeting against produced examples, or lower
`--gold0-fraction` to ~0.45 to land near 66%. Worth doing — it is cheap and
directly concentrates training on the decidable cases.

## 5. Bugs already fixed — do not reintroduce

Eight pods failed before the pipeline held. Each was a distinct real defect:

1. **`dockerStartCmd` swallowed** by the image's init → use `dockerEntrypoint`.
2. **`PATH` lacks Python/git** once the entrypoint is overridden → `bootstrap.sh`
   resolves `python3` explicitly and installs git/curl.
3. **`setuptools<60` is wrong on Python 3.12** (no `distutils`; fails with
   `BackendUnavailable`). Install `python3-hunspell` from apt instead. The
   `setuptools<60` trick is only for Python ≤3.11 (the local box).
4. **`pyarrow` import race** — the corpus download ran concurrently with the pip
   install providing pyarrow → import is now lazy.
5. **`set -u` aborts** while sourcing the image's `.bashrc` → `set -o pipefail` only.
6. **raw.githubusercontent caches branch paths** for minutes → `launch.py` pins
   an exact commit SHA. This also makes runs reproducible.
7. **Parquet schema drift** — pandas infers dtypes per chunk, so a chunk whose
   `cand_i` is entirely null infers `null` not `string` and the next chunk fails
   with `Table schema does not match schema used to create file`. Killed a build
   13% in. Fixed by pinning `example_schema()`.
8. **`vocab_size` 280 vs `MASK_ID` 280** in `model_87m.yaml` — an anchored `sed`
   silently missed the line because it carries a trailing comment. Died as an
   async CUDA gather assert on the full train's first batch, *after* the 23-min
   data build. The sanity train missed it because it uses the 28M config.

9. **CUDA OOM at step 18 of the full train.** microbatch 256 peaked at 44.5 GiB
   of a 44.52 GiB L40S. Not caught at step 1 because **length bucketing makes
   memory vary per batch**: most batches are short and the peak only arrives
   with the first max-length batch. My own pre-run estimate said ~21 GiB and was
   roughly 2x optimistic — the per-layer activation multiplier for a SwiGLU
   block was guessed, not measured. Fixed by microbatch 128 / accum 4 (effective
   batch still 512) plus `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True`.

Lessons from #8 and #9, now enforced in `scripts/preflight.py`, which runs
before the data build:

- it builds **every** training config on the real GPU and takes an optimizer
  step with byte masking live (a sanity check that exercises a *different*
  config than the real run is not a sanity check);
- it uses the **configured microbatch at maximum sequence length**, so peak
  memory is measured rather than estimated, and fails if the peak exceeds 85%
  of the card. Verified to reproduce #8; #9's guard was unverified on GPU at the
  time of writing and was confirmed only by the subsequent successful run.

## 6. Known issues, not yet fixed

- **Task #8 — typo-table sharding depends on CPU count.** `build_typo_table`
  shards by worker count and seeds each shard from its index, so the dataset
  differs between machines (28 vCPU gave 46,825 words/410,555 entries; 112 gave
  46,819/410,594). Same distribution, not byte-identical. The determinism test
  passes only because it pins `workers=1`. Fix: fixed shard size independent of
  CPU count.
- **Task #9 — example target sits at 91% of typo-table capacity**
  (410,555 typos × `max_uses_per_typo=8` = 3.28M vs a 3M target), so generation
  decays from 8.4k/s to <2.5k/s and stops short (2.08M). Efficiency only; the
  build is bounded by `max_passes` and self-terminates. Fix: target ~70% of
  capacity — raise `max_uses_per_typo`, widen the vocabulary, or lower the target.
- **`gold0_fraction` budgets against the target, not the produced count** (see
  §4), leaving easy examples over-represented at 76.6% vs BEA's 66.8%.
- `nproc` inside a Runpod container reports the **host's** cores (112) not the
  allocation (28). `data_build.available_cpus()` handles this; anything new that
  sizes a pool must use it, not `os.cpu_count()`.
- Preflight is slow on CPU at large microbatches (it runs a real 128x448
  forward/backward). It is meant to run on the GPU pod; pass a single small
  config when testing locally.

## 7. Writing up a result

```bash
python scripts/summarize_experiment.py
```

prints the headline table, whether 75% was met, the conditional accuracy that
would have been needed, and splits the residual error into "gold outside pool"
(unreachable without changing candidate generation) vs "model picked wrong"
(reachable by better reranking).

Report the achieved number plainly, including if it misses 75%. The decomposition
is the useful part of the result either way: it says whether the remaining
gap is candidate generation (needs Aspell or a dictionary-search generator, both
currently out of scope) or reranking (needs more model/data).

## 8. Budget

Runpod balance was $9.70 at session start; roughly **$2** spent across nine pods
(eight short-lived failures plus one that reached training). A full run costs
~$2-3 at $0.79/hr, so there is room for two or three more attempts. Check with:

```bash
curl -s -H "Authorization: Bearer $RUNPOD_KEY" -H "Content-Type: application/json" \
  -d '{"query":"query{myself{clientBalance currentSpendPerHr}}"}' \
  https://api.runpod.io/graphql
```
